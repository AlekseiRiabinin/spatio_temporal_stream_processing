{
  "name": "json-to-protobuf-telemetry",
  "config": {
    "connector.class": "io.debezium.connector.kafka.KafkaSourceConnector",
    "tasks.max": "1",

    "kafka.bootstrap.servers": "kafka-1:19092",
    "kafka.topic": "raw-telemetry",

    "value.converter": "io.confluent.connect.protobuf.ProtobufConverter",
    "value.converter.schema.registry.url": "http://schema-registry:8084",

    "transforms": "Hoist",
    "transforms.Hoist.type": "org.apache.kafka.connect.transforms.HoistField$Value",
    "transforms.Hoist.field": "payload",

    "target.topic": "raw-telemetry-proto"
  }
}
